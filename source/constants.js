import { Dimensions } from "react-native";

let all_constants;
export default all_constants = {
    tab : {
        main_tab_navigator: {
            home: "Home",
            pending: "En attente",
            delivery: "Livraison",
        }
    }
}